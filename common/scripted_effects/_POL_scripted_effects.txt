# icon_seats regular political orientation (from left to right)
# [com] [soc] [prog] [soclib] [marktlib] [con] [pat] [desp] [nat] [fasc]

# The following scripted effect sets-up the parliament at game start.
parliament_gui_initial = {
	# Put simply an array stores a list of things, used here to store the list of parties and list of seats.

	# POL_arr_nat_LEG_seats stores the list of seat numbers for each party
	# code in a check to distribute seats according to party popularity if not already distributed later.
	IF = { limit = { tag = SWE } 
		add_to_array = { POL_arr_nat_LEG_seats = 17 } # com
		add_to_array = { POL_arr_nat_LEG_seats = 33 } # soc
		add_to_array = { POL_arr_nat_LEG_seats = 130 } # prog
		add_to_array = { POL_arr_nat_LEG_seats = 71 } # soclib
		add_to_array = { POL_arr_nat_LEG_seats = 58 } # marktlib
		add_to_array = { POL_arr_nat_LEG_seats = 41 } # con
		add_to_array = { POL_arr_nat_LEG_seats = 0 } # pat
		add_to_array = { POL_arr_nat_LEG_seats = 0 } # desp
		add_to_array = { POL_arr_nat_LEG_seats = 0 } # nat
		add_to_array = { POL_arr_nat_LEG_seats = 0 } # fasc
	}
	# (sorry Bernard referencing POL_arr_nat_LEG_seats^x LITERALLY MEANS GETTING THOSE PARTIES' SEATS. WHAT THE FUCK IS WRONG WITH YOU, THAT'S ONE GOOD HOUR YOU FUCKING IDIOT!)
	# coalition setup
	IF = { limit = { tag = SWE } 
		add_to_array = { POL_arr_nat_LEG_parties_coalition = 1 }
			add_to_array = { POL_arr_nat_LEG_parties_support_pct = 0.5 }	# POL_arr_nat_LEG_parties_support_seats
		add_to_array = { POL_arr_nat_LEG_parties_coalition = 2 }
			add_to_array = { POL_arr_nat_LEG_parties_support_pct = 1 }	# POL_arr_nat_LEG_parties_support_seats
		add_to_array = { POL_arr_nat_LEG_parties_coalition = 3 }
			add_to_array = { POL_arr_nat_LEG_parties_support_pct = 1 }	# POL_arr_nat_LEG_parties_support_seats
		add_to_array = { POL_arr_nat_LEG_parties_noncoalition = 4 }
			add_to_array = { POL_arr_nat_LEG_parties_support_pct = 0.2 }	# POL_arr_nat_LEG_parties_support_seats
		add_to_array = { POL_arr_nat_LEG_parties_noncoalition = 5 }
			add_to_array = { POL_arr_nat_LEG_parties_support_pct = 0.1 }	# POL_arr_nat_LEG_parties_support_seats
		add_to_array = { POL_arr_nat_LEG_parties_noncoalition = 6 }
			add_to_array = { POL_arr_nat_LEG_parties_support_pct = 0.05 }	# POL_arr_nat_LEG_parties_support_seats
		add_to_array = { POL_arr_nat_LEG_parties_noncoalition = 7 }
			add_to_array = { POL_arr_nat_LEG_parties_support_pct = 0 }	# POL_arr_nat_LEG_parties_support_seats
		add_to_array = { POL_arr_nat_LEG_parties_noncoalition = 8 }
			add_to_array = { POL_arr_nat_LEG_parties_support_pct = 0 }	# POL_arr_nat_LEG_parties_support_seats
		add_to_array = { POL_arr_nat_LEG_parties_noncoalition = 9 }
			add_to_array = { POL_arr_nat_LEG_parties_support_pct = 0 }	# POL_arr_nat_LEG_parties_support_seats
		add_to_array = { POL_arr_nat_LEG_parties_noncoalition = 10 }
			add_to_array = { POL_arr_nat_LEG_parties_support_pct = 0 }	# POL_arr_nat_LEG_parties_support_seats
	}

	# POL_arr_nat_LEG_parties stores the list of parties
	# This basically assigns an id for each party, so Party A = 1, Party B = 2, etc. (must start from 1 and is in order)
	add_to_array = { POL_arr_nat_LEG_parties = 1 } # com
	add_to_array = { POL_arr_nat_LEG_parties = 2 } # soc
	add_to_array = { POL_arr_nat_LEG_parties = 3 } # prog
	add_to_array = { POL_arr_nat_LEG_parties = 4 } # soclib
	add_to_array = { POL_arr_nat_LEG_parties = 5 } # marktlib
	add_to_array = { POL_arr_nat_LEG_parties = 6 } # con
	add_to_array = { POL_arr_nat_LEG_parties = 7 } # pat
	add_to_array = { POL_arr_nat_LEG_parties = 8 } # desp
	add_to_array = { POL_arr_nat_LEG_parties = 9 } # nat
	add_to_array = { POL_arr_nat_LEG_parties = 10 } # fasc

	# Modify what shows up in the gui
	reload_parliament_gui = yes

	# sets up coalitions
	POL_se_LEG_set_up_coalitions = yes
}

# elections hooray
POL_se_LEG_elections_seat_allocation = {
	#clear_array = POL_arr_nat_LEG_seats
	set_temp_variable = { total_seats_calculated = 0 }

	IF = { limit = { check_variable = { POL_var_nat_LEG_seats_total = 0 } }
		set_variable = { POL_var_nat_LEG_seats_total = 350 } }

	set_variable = { POL_arr_nat_LEG_votes_pct^0 = party_popularity@communism }	# com
	set_variable = { POL_arr_nat_LEG_votes_pct^1 = party_popularity@soc }
	set_variable = { POL_arr_nat_LEG_votes_pct^2 = party_popularity@democratic }	# prog
	set_variable = { POL_arr_nat_LEG_votes_pct^3 = party_popularity@soclib }
	set_variable = { POL_arr_nat_LEG_votes_pct^4 = party_popularity@marktlib }
	set_variable = { POL_arr_nat_LEG_votes_pct^5 = party_popularity@con }
	set_variable = { POL_arr_nat_LEG_votes_pct^6 = party_popularity@neutrality }	# pat
	set_variable = { POL_arr_nat_LEG_votes_pct^7 = party_popularity@desp }
	set_variable = { POL_arr_nat_LEG_votes_pct^8 = party_popularity@nat }
	set_variable = { POL_arr_nat_LEG_votes_pct^9 = party_popularity@fascism }	# fasc

	for_each_loop = { array = POL_arr_nat_LEG_parties
		index = i	value = v

		set_variable = { POL_arr_nat_LEG_seats^i = POL_var_nat_LEG_seats_total }
			multiply_variable = { POL_arr_nat_LEG_seats^i = POL_arr_nat_LEG_votes_pct^i }
			round_variable = POL_arr_nat_LEG_seats^i
			add_to_temp_variable = { total_seats_calculated = POL_arr_nat_LEG_seats^i } }
	
	IF = { limit = { check_variable = { total_seats_calculated > POL_var_nat_LEG_seats_total } }
		subtract_from_temp_variable = { total_seats_calculated = POL_var_nat_LEG_seats_total }
		for_each_loop = { array = POL_arr_nat_LEG_seats
			index = i	value = v	break = b
			IF = { limit = { check_variable = { POL_arr_nat_LEG_seats^i > 0 } }
				subtract_from_variable = { POL_arr_nat_LEG_seats^i = total_seats_calculated }
				set_temp_variable = { b = 1 } }
		} }
	ELSE_IF = { limit = { check_variable = { total_seats_calculated < POL_var_nat_LEG_seats_total } }
		subtract_from_temp_variable = { total_seats_calculated = POL_var_nat_LEG_seats_total }
		multiply_temp_variable = { total_seats_calculated = -1 }
		for_each_loop = { array = POL_arr_nat_LEG_seats
			index = i	value = v	break = b
			IF = { limit = { check_variable = { POL_arr_nat_LEG_seats^i > 0 } }
				add_to_variable = { POL_arr_nat_LEG_seats^i = total_seats_calculated }
				set_temp_variable = { b = 1 } }
		} }
	
	clear_variable = total_seats_calculated
	reload_parliament_gui = yes
}

# call this to initiate parliamentary votes
POL_se_LEG_vote = {
	# define two things first:
		# - bill index (POL_arr_nat_LEG_laws_index)	[do NOT bypass]
		# - passing threshold (POL_var_nat_LEG_laws_threshold_pct) [can bypass if threshold = 0.5]
		# - days voting (POL_var_nat_LEG_laws_days_voting) [can bypass if = 30 days]
		set_variable = { POL_var_nat_LEG_laws_passed = 0 }
		IF = { limit = { check_variable = { POL_arr_nat_LEG_laws_threshold_pct = 0 } } 
			set_variable = { POL_arr_nat_LEG_laws_threshold_pct = 0.5 } }
		IF = { limit = { check_variable = { POL_var_nat_LEG_laws_days_voting = 0 } } 
			set_variable = { POL_var_nat_LEG_laws_days_voting = 30 } }

	# leg process
	  	# sets a country flag at the beginning of the voting process
			set_country_flag = is_voting
	  	# set passing var
	  		set_variable = { POL_var_nat_LEG_seats_pass_required = POL_var_nat_LEG_seats_total }
			multiply_variable = { POL_var_nat_LEG_seats_pass_required = POL_arr_nat_LEG_laws_threshold_pct }	round_variable = POL_var_nat_LEG_seats_pass_required
	    	activate_mission = POL_mission_LEG_vote_timeout
	  
}
# everything during the vote goes into another scripted effect

# everything after the vote goes here
POL_se_LEG_vote_finished = {
	# if passed? if not?
	# clear the voting country flag first
		clr_country_flag = is_voting
	# check if current support is over required support
		set_variable = { POL_var_nat_LEG_parties_support_seats = POL_var_nat_LEG_parties_noncoalition_support_seats }	add_to_variable = { POL_var_nat_LEG_parties_support_seats = POL_var_nat_LEG_parties_coalition_support_seats }
		IF = { limit = { check_variable = { POL_var_nat_LEG_parties_support_seats > POL_var_nat_LEG_seats_pass_required } }
			set_variable = { POL_var_nat_LEG_laws_passed = 1 } }
	# effects
		IF = { limit = { check_variable = { POL_var_nat_LEG_laws_passed = 1 } }	# This piece of legislation has cleared the Legislature.
			for_each_loop = { array = POL_arr_nat_LEG_laws_index
				index = i	value = v

				#log = "[?i], [?v], [?POL_arr_nat_LEG_laws_index^i], [?POL_arr_nat_LEG_laws_index^v]"

				# test law
					IF = { limit = { check_variable = { POL_arr_nat_LEG_laws_index^i = 1 } }
						THIS = { add_political_power = 100 }
					}
				# 
					ELSE_IF = { limit = { check_variable = { POL_arr_nat_LEG_laws_index^i = 2 } } 
						THIS = { POL_policies_se_set = yes }
					}
			}
		# test law
		#	IF = { limit = { check_variable = { POL_var_nat_LEG_laws_index = 1 } }
		#		THIS = { add_political_power = 100 }
		#	}

		#	ELSE_IF = { limit = { check_variable = { POL_var_nat_LEG_laws_index = 2 } } 
		#		THIS = {  
		#			POL_policies_se_set = yes
		#		}
		#	}
		}
		ELSE = {	# Something happened that meant this piece hasn't
			for_each_loop = { array = POL_arr_nat_LEG_laws_index
				index = i	value = v

				# test law
					IF = {  limit = { check_variable = { POL_arr_nat_LEG_laws_index^i = 1 } }
						THIS = { add_political_power = -100 }
					}
				# 
					ELSE_IF = { limit = { check_variable = { POL_arr_nat_LEG_laws_index^i = 2 } } 
						THIS = { custom_effect_tooltip = haha }
					}
			}
		# test law
		#	IF = { limit = { check_variable = { POL_var_nat_LEG_laws_index = 1 } }
		#		THIS = { add_political_power = -100 }
		#	}
		}
	clear_variable = POL_var_nat_LEG_laws_index
	clear_variable = POL_arr_nat_LEG_laws_threshold_pct
	clear_variable = POL_var_nat_LEG_laws_days_voting

}
POL_se_LEG_vote_test_cursed = { 
	clear_array = POL_arr_nat_LEG_laws_index

	add_to_array = { POL_arr_nat_LEG_laws_index = 1 }
	add_to_array = { POL_arr_nat_LEG_laws_index = 3 }
	#set_variable = { POL_var_nat_LEG_laws_index = 1 } 
	set_variable = { POL_arr_nat_LEG_laws_threshold_pct = 0.5 }
	POL_se_LEG_vote = yes }

# clear POL_arr_nat_LEG_parties_coalition before calling this
# cannibalise this for other codes as well
POL_se_LEG_set_up_coalitions = {
	clear_variable = POL_var_nat_LEG_seats_total
	clear_variable = POL_var_nat_LEG_seats_coalition
	clear_variable = POL_var_nat_LEG_parties_coalition_support_seats

	for_each_loop = { array = POL_arr_nat_LEG_parties_coalition
		index = i	value = v

		# visual

		# mathematics
		set_temp_variable = { v1 = v }	subtract_from_temp_variable = { v1 = 1 }
		add_to_variable = { POL_var_nat_LEG_seats_coalition = POL_arr_nat_LEG_seats^v1 }	# base 0
		set_variable = { POL_arr_nat_LEG_parties_support_seats^v1 = POL_arr_nat_LEG_seats^v1 }
			multiply_variable = { POL_arr_nat_LEG_parties_support_seats^v1 = POL_arr_nat_LEG_parties_support_pct^v1 }
			round_variable = POL_arr_nat_LEG_parties_support_seats^v1
			add_to_variable = { POL_var_nat_LEG_parties_coalition_support_seats = POL_arr_nat_LEG_parties_support_seats^v1 }
	}

	for_each_loop = { array = POL_arr_nat_LEG_parties_noncoalition
		index = i	value = v

		# visual

		# mathematics
		set_temp_variable = { v1 = v }	subtract_from_temp_variable = { v1 = 1 }
		add_to_variable = { POL_var_nat_LEG_seats_noncoalition = POL_arr_nat_LEG_seats^v1 }	# base 0
		set_variable = { POL_arr_nat_LEG_parties_support_seats^v1 = POL_arr_nat_LEG_seats^v1 }
			multiply_variable = { POL_arr_nat_LEG_parties_support_seats^v1 = POL_arr_nat_LEG_parties_support_pct^v1 }
			round_variable = POL_arr_nat_LEG_parties_support_seats^v1
			add_to_variable = { POL_var_nat_LEG_parties_noncoalition_support_seats = POL_arr_nat_LEG_parties_support_seats^v1 }
	}
	for_each_loop = { array = POL_arr_nat_LEG_parties
		index = i	value = v

		set_temp_variable = { v1 = v }	subtract_from_temp_variable = { v1 = 1 }
		add_to_variable = { POL_var_nat_LEG_seats_total = POL_arr_nat_LEG_seats^v1 }	# base 0
	}

	set_variable = { POL_var_nat_LEG_parties_support_seats = POL_var_nat_LEG_parties_noncoalition_support_seats }	add_to_variable = { POL_var_nat_LEG_parties_support_seats = POL_var_nat_LEG_parties_coalition_support_seats }
	set_variable = { POL_var_nat_LEG_seats_half = POL_var_nat_LEG_seats_total }	divide_variable = { POL_var_nat_LEG_seats_half = 2 }	round_variable = POL_var_nat_LEG_seats_half
	set_variable = { POL_var_nat_LEG_seats_3qtr = POL_var_nat_LEG_seats_total }	divide_variable = { POL_var_nat_LEG_seats_3qtr = 1.5 }	round_variable = POL_var_nat_LEG_seats_3qtr
	clear_variable = v1
}

# The following scripted effect does the main job of generating the gui, by adding the seats for each party in a grid.
# This should be called every time you need to update the gui, when you add/subtract seats for example.
# Read the comments if you're interested in how it works
reload_parliament_gui = {
	# Clears what's currently on the gui, if there's anything there.
	clear_array = POL_arr_nat_LEG_disp

	# The array temp_seat is duplicated from the array POL_arr_nat_LEG_seats.
	for_each_loop = {
		array = POL_arr_nat_LEG_seats

		add_to_array = { temp_seat = POL_arr_nat_LEG_seats^i }
	}

	# It's a suprise tool for later
	set_temp_variable = { temp_index_1 = 0 }
	set_temp_variable = { temp_index_2 = 1 }

	# Set temp_size as the highest index in the array, highest index is the highest value x in temp_array^x.
	set_temp_variable = { temp_size = temp_seat^num }
	subtract_from_temp_variable = { temp_size = 1 }

	# What this does is loop through the temp_seat array, adding the current value to the next value on the list.
	# temp_seat^0 is the first value on the list, temp_seat^1 is the next and so forth.
	# the loop ends when temp_index_1 is equal to the size of the array - 1 (basically the max index).
	while_loop_effect = {
		limit = {
			check_variable = { temp_index_1 < temp_size }
		}
		# The suprise tool starts out as temp_seat^0 and temp_seat^1 and then is incremented every loop , adding the current value to the next value on the list.
		add_to_variable = { temp_seat^temp_index_2 = temp_seat^temp_index_1 }
		add_to_temp_variable = { temp_index_1 = 1 }
		add_to_temp_variable = { temp_index_2 = 1 }
	}

	# resize_array fills the POL_arr_nat_LEG_parties with the id of the parliament member x amount of times where x is the number of seats the member has.
	# so the array will look like this basically:
	# 1,1,1,1,1,1,1,...(repeated 90 times),2,2,2,2,2,2,2(repeated 35 times),3,3,3.. etc.
	for_loop_effect = {
		start = 0
		end = temp_seat^num

		resize_array = { array = POL_arr_nat_LEG_disp value = POL_arr_nat_LEG_parties^v size = temp_seat^v }
	}

	# Clear the temp_seat array used previously since it's not needed anymore.
	clear_array = temp_seat
}

# Bonus code for adding/subtracting seats
modify_parliament_seat = {
	subtract_from_temp_variable = { temp_member = 1 }

	# check to see if result overflows
	set_temp_variable = { temp = POL_arr_nat_LEG_seats^temp_member }
	add_to_temp_variable = { temp = temp_seats }

	if = {
		limit = {
			check_variable = { temp > POL_arr_nat_LEG_disp^num }
		}
		subtract_from_temp_variable = { temp = POL_arr_nat_LEG_disp^num }
		subtract_from_temp_variable = { temp_seats = temp }
	}
	else_if = {
		limit = {
			check_variable = { temp < 0 }
		}
		multiply_temp_variable = { temp = -1 }
		subtract_from_temp_variable = { temp_seats = temp }
	}

	# adds all parties to temp_array
	for_loop_effect = {
		start = 0
		end = POL_arr_nat_LEG_seats^num

		add_to_temp_array = { temp_array = v }
	}

	# remove from list of parties the party to be added/subtracted
	remove_from_temp_array = { array = temp_array value = temp_member }

	# give party new seat value
	add_to_variable = { POL_arr_nat_LEG_seats^temp_member = temp_seats }

	# if its add
	if = {
		limit = {
			check_variable = { temp_seats > 0 }
		}
		# loop until seats to be added is 0
		while_loop_effect = {
			limit = {
				check_variable = { temp_seats > 0 }
			}

			# loops through remaining parties, subtracting 1 seat each time
			for_loop_effect = {
				start = 0
				end = temp_array^num

				if = {
					limit = {
						check_variable = { POL_arr_nat_LEG_seats^temp_array^v > 0 } # party must have more than 0 seats to be subtracted
						check_variable = { temp_seats > 0 } # seats to be added is bigger than 0 currently
					}
					subtract_from_variable = { POL_arr_nat_LEG_seats^temp_array^v = 1 } # subtract from party 1 seat
					subtract_from_temp_variable = { temp_seats = 1 } # subtract from seats to be added
				}
			}
		}
	}
	# if its subtract
	else_if = {
		limit = {
			check_variable = { temp_seats < 0 }
		}
		# loop until seats to be subtracted is 0
		while_loop_effect = {
			limit = {
				check_variable = { temp_seats < 0 }
			}

			# loops through remaining parties, adding 1 seat each time
			for_loop_effect = {
				start = 0
				end = temp_array^num

				if = {
					limit = {
						check_variable = { POL_arr_nat_LEG_seats^temp_array^v < POL_arr_nat_LEG_disp^num } # party must have less than max seats to be added
						check_variable = { temp_seats < 0 } # seats to be subtracted is less than 0 currently
					}
					add_to_variable = { POL_arr_nat_LEG_seats^temp_array^v = 1 } # add to party 1 seat
					add_to_temp_variable = { temp_seats = 1 } # add to seats to be subtracted
				}
			}
		}
	}
	# do nothing if 0

	# Modify what shows up in the gui
	reload_parliament_gui = yes
}

# Clear all arrays (for debugging)
clear_parliament_gui_arrays = {
	clear_array = POL_arr_nat_LEG_disp
	clear_array = POL_arr_nat_LEG_parties
	clear_array = POL_arr_nat_LEG_seats
}
